{
  "name": "omnichannel-iam-service",
  "version": "1.0.0",
  "description": "Identity & Access Management (IAM) microservice for the Omni-Channel platform. Owns the user store (MongoDB), issues/verifies JWTs signed with JWT_ACCESS_SECRET (must match the API Gateway), and implements the SLT OMNI legacy auth contract (register, login, OTP, refresh, password reset, external login, profile update, terminate).",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node src/utils/seedAdmin.js"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.19.2",
    "helmet": "^7.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.5.1",
    "morgan": "^1.10.0",
    "uuid": "^9.0.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.4",
    "supertest": "^7.2.2"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "license": "UNLICENSED",
  "private": true
}
